# Content Generator Helpers

This folder contains optional helper scripts and notebooks for generating publication or talk entries from TSV data.

Current repository workflow:

- The live site content in `_publications/` and `_talks/` is the source of truth.
- The scripts here are only for bulk generation or migration.
- If these scripts are used, review the generated Markdown before committing it.

Files in this folder are excluded from the published site.
